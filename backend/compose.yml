version: '1.0'
services:
  business:
    build: ./business_logic
    ports:
      - "8000:8000" 
    env_file: .env.dev
    depends_on:
      - routing
      - postgres

  routing:
    build: ./routing
    ports: ["8001:8000"]     
    env_file: .env.dev
    deploy:
      replicas: 3